<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginBottom="8dp"
    android:padding="10dp"
    android:background="@drawable/bg_planner_card"
    android:foreground="?attr/selectableItemBackground">

    <androidx.cardview.widget.CardView
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:cardCornerRadius="11dp"
        app:cardElevation="0dp">
        <ImageView
            android:id="@+id/img_planner_meal_thumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/meal_image"/>
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_planner_meal_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:textAppearance="@style/Text.Subtitle"
        android:maxLines="2"
        android:ellipsize="end"/>

    <ImageButton
        android:id="@+id/btn_planner_meal_delete"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:padding="7dp"
        android:src="@drawable/ic_delete"
        android:scaleType="fitCenter"
        android:background="@drawable/bg_btn_delete"
        app:tint="@color/del_color"
        android:contentDescription="@string/delete"/>
</LinearLayout>